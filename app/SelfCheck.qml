// Thin entry point — delegates to the modular harness (Phase 30).
// main.cpp still loads this file; behavior identical to the monolithic harness.
import QtQuick
import "../src/selfcheck"

SelfCheckRunner {
}
